"use client"

import { useTranslations } from 'next-intl'

export default function DashboardPage() {
  // El contenido principal se renderiza en el CleanDashboard cuando pathname incluye '/dashboard'
  // Esta página solo se muestra si el componente CleanDashboard no renderiza el contenido
  return null
}
